# pseudoCode

- Define a function findHiddentWord that takes an argument
    - for loop to iterate through each letter
      - var hiddenWord = " 
        for every letter
            use charAt method to take out a single letter from the crowd
            check if the letter upper case and lower case.
                convert the letter to uppercase and compare it with the original.
                    use the upperCase() method
                if equal then it upper case
                    Skip
                else 
                    save the value on the hiddenWord variable

            Return the findHiddenWord varible

              
